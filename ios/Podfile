# ios/Podfile 顶部添加
source 'https://mirrors.tuna.tsinghua.edu.cn/git/CocoaPods/Specs.git'

# Resolve react_native_pods.rb with node to allow for hoisting
require Pod::Executable.execute_command('node', ['-p',
  'require.resolve(
    "react-native/scripts/react_native_pods.rb",
    {paths: [process.argv[1]]},
  )', __dir__]).strip

platform :ios, '13'

use_modular_headers!
prepare_react_native_project!

# If you are using a `react-native-flipper` your iOS build will fail when `NO_FLIPPER=1` is set.
# because `react-native-flipper` depends on (FlipperKit,...) that will be excluded
#
# To fix this you can also exclude `react-native-flipper` using a `react-native.config.js`
# ```js
# module.exports = {
#   dependencies: {
#     ...(process.env.NO_FLIPPER ? { 'react-native-flipper': { platforms: { ios: null } } } : {}),
# ```
flipper_config = ENV['NO_FLIPPER'] == "1" ? FlipperConfiguration.disabled : FlipperConfiguration.enabled

linkage = ENV['USE_FRAMEWORKS']
if linkage != nil
  Pod::UI.puts "Configuring Pod with #{linkage}ally linked Frameworks".green
  # use_frameworks! :linkage => linkage.to_sym
  use_frameworks! :linkage => :static
end

target 'AwesomeProject' do
  config = use_native_modules!

  # Flags change depending on the env values.
  flags = get_default_flags()

  use_react_native!(
    :path => config[:reactNativePath],
    # Hermes is now enabled by default. Disable by setting this flag to false.
    :hermes_enabled => flags[:hermes_enabled],
    :fabric_enabled => flags[:fabric_enabled],
    # Enables Flipper.
    #
    # Note that if you have use_frameworks! enabled, Flipper will not work and
    # you should disable the next line.
    # :flipper_configuration => flipper_config,
    # An absolute path to your application root.
    :app_path => "#{Pod::Config.instance.installation_root}/.."
  )

  target 'AwesomeProjectTests' do
    inherit! :complete
    # 核心配置：强制所有Pods使用静态框架（兼容Flipper静态库）
    use_frameworks! :linkage => :static
    # Pods for testing
    # 手动指定 Boost 版本
    # 覆盖默认的 Boost 引用
    # pod 'Boost', :podspec => '../node_modules/react-native/third-party-podspecs/Boost.podspec', :version => '1.76.0'
    # 写法1：直接为FirebaseSessions配置（简洁高效，自动覆盖所有子依赖）
    # pod 'FirebaseSessions', :modular_headers => true
    # 写法2：为所有缺失模块逐个配置（精准控制，适合特殊场景）
    pod 'FirebaseCore', :modular_headers => true
    pod 'FirebaseCoreExtension', :modular_headers => true
    pod 'FirebaseInstallations', :modular_headers => true
    pod 'GoogleDataTransport', :modular_headers => true
    pod 'GoogleUtilities', :modular_headers => true
    pod 'nanopb', :modular_headers => true
    pod 'FirebaseSessions' # 最后引入FirebaseSessions
  end

  post_install do |installer|
    # https://github.com/facebook/react-native/blob/main/packages/react-native/scripts/react_native_pods.rb#L197-L202
    react_native_post_install(
      installer,
      config[:reactNativePath],
      :mac_catalyst_enabled => false
    )

      # 遍历所有Pods Target，统一设置IPHONEOS_DEPLOYMENT_TARGET
    installer.pods_project.targets.each do |target|
      # 覆盖所有Pods的部署目标版本（与platform :ios指定的版本一致即可）
      target.build_configurations.each do |config|
        config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0' # 此处需与上方platform版本一致，最低12.0
      end

      # 可选：单独针对RNCPushNotificationIOS-library设置（精准控制，可省略，上述批量配置已覆盖）
      # if target.name == 'RNCPushNotificationIOS-library'
      #   target.build_configurations.each do |config|
      #     config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
      #   end
      # end
    end
    __apply_Xcode_12_5_M1_post_install_workaround(installer)
  end
end
